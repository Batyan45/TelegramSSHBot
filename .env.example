# --- Telegram ---
BOT_TOKEN=123456:ABC-DEF...     # токен бота
ALLOWED_USER_IDS=111111111,222222222

# --- SSH (remote host) ---
SSH_HOST=main-server.lan
SSH_PORT=22
SSH_USER=batyan
SSH_TIMEOUT_SEC=25

# Приватный ключ в base64 (чтобы удобно класть в .env).
# Пример: base64 -w0 id_ed25519 > id_ed25519.b64
SSH_KEY_BASE64=LS0tLS1CRUdJTiB... (обрезано)

# Защита от MITM: добавьте строку из `ssh-keyscan -p 22 main-server.lan`
# Если оставить пустым — StrictHostKeyChecking=accept-new
SSH_KNOWN_HOSTS_LINE=main-server.lan ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAI... (обрезано)
